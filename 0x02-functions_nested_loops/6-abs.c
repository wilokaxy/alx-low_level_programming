#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: Integer input
 * Return: Integer (Success)
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}	
